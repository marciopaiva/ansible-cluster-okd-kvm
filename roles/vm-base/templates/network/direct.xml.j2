<network>
  <name>{{ network.name }}</name>
{% if network.uuid|d() %}
  <uuid>{{ network.uuid }}</uuid>
{% endif %}
  <forward mode="bridge">
    <interface dev="{{ network.bridge }}" />
  </forward>
</network>